import { PathCompletePipe } from './path-complete.pipe';

describe('PathCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new PathCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
